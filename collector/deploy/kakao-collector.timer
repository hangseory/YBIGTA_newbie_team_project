[Unit]
Description=Run kakao-collector.service every hour

[Timer]
# 부팅 2분 후 첫 실행, 이후 30분 간격으로 반복
OnBootSec=2min
OnUnitActiveSec=30min
# EC2가 재시작 등으로 실행 시각을 놓쳤어도 다음 부팅 시 바로 실행
Persistent=true

[Install]
WantedBy=timers.target
